# 文本处理

<cite>
**本文档引用的文件**
- [README.md](file://README.md)
- [config.example.json](file://config.example.json)
- [xiaowang.py](file://xiaowang.py)
- [llm.py](file://llm.py)
- [tools.py](file://tools.py)
- [router.py](file://router.py)
- [memory.py](file://memory.py)
- [scheduler.py](file://scheduler.py)
- [mcp_client.py](file://mcp_client.py)
- [self_check_tool.py](file://self_check_tool.py)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

724 Office 是一个基于纯Python构建的AI智能体系统，专为24/7运行而设计。该系统实现了完整的文本处理功能，包括消息去抖动、文本分片、多媒体处理和响应发送等核心能力。

系统的核心特性包括：
- **零框架依赖**：完全使用标准库和少量小包
- **多租户路由**：支持多个用户的独立容器实例
- **文本去抖动机制**：智能合并连续的消息输入
- **文本分片功能**：自动处理长文本的UTF-8编码分段
- **多媒体处理**：支持图片、视频、语音、文件等多种媒体类型
- **内存系统**：三层记忆架构（短期、长期、检索）

## 项目结构

系统采用模块化设计，每个文件负责特定的功能领域：

```mermaid
graph TB
subgraph "入口层"
XW[xiaowang.py<br/>主入口点]
RT[router.py<br/>多租户路由]
end
subgraph "核心处理层"
LLM[llm.py<br/>LLM调用循环]
TM[tools.py<br/>工具注册表]
MC[mcp_client.py<br/>MCP客户端]
end
subgraph "数据层"
MEM[memory.py<br/>三层记忆系统]
SCH[scheduler.py<br/>调度器]
end
subgraph "配置层"
CFG[config.example.json<br/>配置示例]
DOC[README.md<br/>项目文档]
end
XW --> LLM
XW --> TM
XW --> MEM
XW --> SCH
RT --> XW
TM --> MC
LLM --> MEM
```

**图表来源**
- [xiaowang.py:1-626](file://xiaowang.py#L1-L626)
- [router.py:1-493](file://router.py#L1-L493)
- [llm.py:1-401](file://llm.py#L1-L401)
- [tools.py:1-800](file://tools.py#L1-L800)

**章节来源**
- [README.md:1-162](file://README.md#L1-L162)
- [config.example.json:1-61](file://config.example.json#L1-L61)

## 核心组件

### 文本去抖动机制

系统实现了智能的消息去抖动功能，通过时间窗口聚合连续的消息输入：

```mermaid
sequenceDiagram
participant Client as 客户端
participant Debounce as 去抖动缓冲区
participant Timer as 计时器
participant LLM as LLM处理器
participant Messaging as 消息发送器
Client->>Debounce : 发送消息1
Debounce->>Timer : 启动3秒计时器
Client->>Debounce : 发送消息2 (3秒内)
Debounce->>Timer : 取消旧计时器
Debounce->>Timer : 启动新计时器
Timer-->>Debounce : 超时触发
Debounce->>Debounce : 合并消息
Debounce->>LLM : 处理合并后的消息
LLM->>Messaging : 发送响应
Messaging-->>Client : 返回响应
```

**图表来源**
- [xiaowang.py:316-378](file://xiaowang.py#L316-L378)

### 文本分片功能

系统支持UTF-8编码的智能文本分片，确保消息长度不超过平台限制：

```mermaid
flowchart TD
Start([开始分片]) --> CheckLen{"检查UTF-8字节数<br/><= 1800?"}
CheckLen --> |是| ReturnSingle["返回单个片段"]
CheckLen --> |否| InitVars["初始化变量<br/>chunks=[], current=''"]
InitVars --> SplitLines["按换行符分割文本"]
SplitLines --> ProcessLine["逐行处理"]
ProcessLine --> TestChunk{"测试新片段<br/>长度> 1800?"}
TestChunk --> |是| AddToChunks["添加当前片段到列表"]
TestChunk --> |否| UpdateCurrent["更新当前片段"]
AddToChunks --> ResetCurrent["current = 行内容"]
UpdateCurrent --> NextLine{"还有下一行?"}
ResetCurrent --> NextLine
NextLine --> |是| ProcessLine
NextLine --> |否| Finalize["添加最后片段"]
Finalize --> ReturnChunks["返回分片数组"]
ReturnSingle --> End([结束])
ReturnChunks --> End
```

**图表来源**
- [xiaowang.py:291-305](file://xiaowang.py#L291-L305)
- [tools.py:91-105](file://tools.py#L91-L105)

### 多媒体消息处理

系统支持多种媒体类型的智能处理：

| 媒体类型 | 类型码 | 处理流程 |
|---------|--------|----------|
| 普通文本 | 0 | 直接文本处理 |
| 图片 | 7, 14, 101 | 下载 → 编码 → 保存 → 视觉分析 |
| 视频 | 22, 23, 103 | 下载 → 保存 → 视频处理 |
| 文件 | 15, 20, 102 | 下载 → 保存 → 文件操作 |
| GIF | 29, 104 | 下载 → 保存 → 图像处理 |
| 语音 | 16 | 下载 → ASR转文本 → 文本处理 |
| 链接分享 | 13 | 提取标题和URL → 文本处理 |
| 位置信息 | 6 | 提取标签和地址 → 文本处理 |

**章节来源**
- [xiaowang.py:489-554](file://xiaowang.py#L489-L554)
- [xiaowang.py:383-465](file://xiaowang.py#L383-L465)

## 架构概览

系统采用分层架构设计，确保各组件职责清晰且松耦合：

```mermaid
graph TB
subgraph "网络接口层"
HTTP[HTTP服务器]
WS[WebSocket连接]
end
subgraph "消息处理层"
CB[回调处理器]
DEB[去抖动引擎]
MEDIA[媒体下载器]
end
subgraph "业务逻辑层"
LLM[LLM对话引擎]
TOOLS[工具执行器]
MEMORY[记忆系统]
SCHED[调度器]
end
subgraph "外部集成层"
MSG[消息平台API]
ASR[语音识别API]
EMB[嵌入向量API]
MCP[MCP服务器]
end
HTTP --> CB
WS --> CB
CB --> DEB
CB --> MEDIA
DEB --> LLM
MEDIA --> LLM
LLM --> TOOLS
TOOLS --> MSG
TOOLS --> ASR
TOOLS --> MCP
LLM --> MEMORY
SCHED --> LLM
MEMORY --> EMB
```

**图表来源**
- [xiaowang.py:559-592](file://xiaowang.py#L559-L592)
- [llm.py:317-401](file://llm.py#L317-L401)
- [tools.py:134-146](file://tools.py#L134-L146)

## 详细组件分析

### 去抖动缓冲区管理

去抖动机制通过线程安全的数据结构实现：

```mermaid
classDiagram
class DebounceBuffer {
+dict buffers : 存储每个用户的缓冲区
+dict timers : 存储定时器引用
+Lock lock : 线程锁
+float debounceSeconds : 去抖动时间间隔
+bufferMessage(sender_id, text, images)
+flushBuffer(sender_id)
+cancelTimer(sender_id)
}
class MessageFragment {
+string text : 文本内容
+list images : 图片路径列表
+datetime timestamp : 时间戳
}
class TimerManager {
+Timer timer : 定时器对象
+startTimer(seconds)
+cancelTimer()
}
DebounceBuffer --> MessageFragment : "管理"
DebounceBuffer --> TimerManager : "控制"
MessageFragment --> TimerManager : "关联"
```

**图表来源**
- [xiaowang.py:311-378](file://xiaowang.py#L311-L378)

### 文本分片算法实现

分片算法确保UTF-8编码的正确性和完整性：

```mermaid
flowchart TD
Input[输入文本] --> Encode[UTF-8编码]
Encode --> CheckSize{检查字节数}
CheckSize --> |<= 1800| ReturnOriginal[返回原文本]
CheckSize --> |> 1800| InitState[初始化状态]
InitState --> SplitByLine[按换行符分割]
SplitByLine --> IterateLines[遍历每一行]
IterateLines --> TestLength{测试新片段长度}
TestLength --> |<= 1800| AppendToCurrent[追加到当前片段]
TestLength --> |> 1800| FlushCurrent[刷新当前片段]
FlushCurrent --> StartNew[开始新片段]
AppendToCurrent --> NextLine{是否有下一行}
StartNew --> NextLine
NextLine --> |是| IterateLines
NextLine --> |否| FinalFlush[刷新最后片段]
FinalFlush --> ReturnChunks[返回分片数组]
ReturnOriginal --> End[结束]
ReturnChunks --> End
```

**图表来源**
- [tools.py:91-105](file://tools.py#L91-L105)

### LLM对话循环

系统实现了强大的工具使用循环，支持多轮对话和工具调用：

```mermaid
sequenceDiagram
participant User as 用户
participant LLM as LLM处理器
participant Tools as 工具执行器
participant Memory as 记忆系统
participant Response as 响应生成器
User->>LLM : 用户消息
LLM->>Memory : 检索相关记忆
Memory-->>LLM : 返回记忆上下文
LLM->>LLM : 生成回复和工具调用
alt 需要工具调用
LLM->>Tools : 执行工具
Tools-->>LLM : 返回工具结果
LLM->>LLM : 继续对话循环
end
LLM->>Response : 格式化最终回复
Response-->>User : 返回最终响应
```

**图表来源**
- [llm.py:317-401](file://llm.py#L317-L401)

**章节来源**
- [llm.py:306-401](file://llm.py#L306-L401)

### 工具系统架构

工具系统提供了灵活的扩展机制：

```mermaid
classDiagram
class ToolRegistry {
+dict registry : 工具注册表
+getDefinitions() list : 获取工具定义
+execute(name, args, ctx) string : 执行工具
}
class ToolDecorator {
+string name : 工具名称
+string description : 工具描述
+dict properties : 参数模式
+list required : 必需参数
+decorator(fn) callable : 装饰器函数
}
class ToolFunction {
+string name : 函数名
+callable fn : 实际函数
+dict definition : OpenAI格式定义
}
ToolRegistry --> ToolDecorator : "使用"
ToolDecorator --> ToolFunction : "包装"
ToolFunction --> ToolRegistry : "注册"
```

**图表来源**
- [tools.py:36-74](file://tools.py#L36-L74)

**章节来源**
- [tools.py:1-800](file://tools.py#L1-L800)

### 多租户路由系统

系统支持多用户的独立容器实例：

```mermaid
flowchart TD
Request[请求到达] --> ParseSender{解析sender_id}
ParseSender --> |已知用户| LookupRoute[查找路由表]
ParseSender --> |未知用户| ProvisionContainer[自动创建容器]
LookupRoute --> ForwardRequest[转发请求]
ProvisionContainer --> WaitHealth[等待健康检查]
WaitHealth --> SendWelcome[发送欢迎消息]
SendWelcome --> ForwardRequest
ForwardRequest --> ReturnResponse[返回响应]
```

**图表来源**
- [router.py:397-418](file://router.py#L397-L418)

**章节来源**
- [router.py:1-493](file://router.py#L1-L493)

## 依赖关系分析

系统采用松耦合的设计，主要依赖关系如下：

```mermaid
graph TB
subgraph "核心依赖"
STD[标准库] --> XW[xiaowang.py]
STD --> LLM[llm.py]
STD --> TM[tools.py]
STD --> MEM[memory.py]
STD --> SCH[scheduler.py]
end
subgraph "第三方包"
CRON[croniter] --> SCH
LANCE[lancedb] --> MEM
WS[websocket-client] --> XW
end
subgraph "外部服务"
MSG_API[消息平台API] --> XW
ASR_API[语音识别API] --> XW
EMB_API[嵌入向量API] --> MEM
LLM_API[LLM API] --> LLM
end
XW --> MSG_API
XW --> ASR_API
LLM --> LLM_API
MEM --> EMB_API
```

**图表来源**
- [README.md:10-22](file://README.md#L10-L22)
- [config.example.json:14-18](file://config.example.json#L14-L18)

**章节来源**
- [README.md:101-138](file://README.md#L101-L138)

## 性能考虑

### 内存优化策略

1. **会话截断**：最多保留40条消息，超出部分自动压缩到长期记忆
2. **图像内容清理**：保存会话时将图像URL替换为标记，减少存储空间
3. **异步内存压缩**：后台线程处理压缩任务，不影响主线程性能

### 并发处理

1. **线程安全**：使用锁保护共享资源，避免竞态条件
2. **异步I/O**：网络请求使用非阻塞方式，提高吞吐量
3. **容器化隔离**：每个用户独立容器，避免资源争用

### 缓存机制

1. **零延迟缓存**：硬件/语音通道使用预计算的记忆摘要
2. **响应缓存**：最近的计划任务输出注入系统提示词中
3. **容器健康检查**：自动监控和重启异常容器

## 故障排除指南

### 常见问题及解决方案

| 问题类型 | 症状 | 解决方案 |
|---------|------|----------|
| 去抖动失效 | 消息频繁触发 | 检查debounce_seconds配置，调整为合适值 |
| 文本分片错误 | 长文本截断 | 验证UTF-8编码，检查最大字节数设置 |
| 媒体下载失败 | 文件无法保存 | 检查网络连接和存储权限 |
| LLM调用超时 | 对话无响应 | 增加超时时间，检查API密钥有效性 |
| 容器启动失败 | 用户无法访问 | 检查Docker配置和资源限制 |

### 调试技巧

1. **启用详细日志**：查看`/var/log/agent.log`获取详细错误信息
2. **监控系统指标**：使用`/health`端点检查系统状态
3. **测试工具链**：使用`/test`端点验证LLM连接
4. **内存使用分析**：定期检查内存占用情况

**章节来源**
- [xiaowang.py:597-626](file://xiaowang.py#L597-L626)

## 结论

724 Office文本处理系统展现了优秀的工程实践，通过以下关键特性实现了高效可靠的文本处理：

1. **智能化的消息去抖动**：通过时间窗口聚合连续输入，显著减少重复处理
2. **精确的文本分片算法**：确保UTF-8编码的完整性和平台兼容性
3. **多模态消息处理**：统一处理文本、图片、视频、语音等多种媒体类型
4. **可扩展的工具系统**：支持动态加载和热重载自定义工具
5. **健壮的多租户架构**：每个用户独立容器，确保资源隔离和稳定性

系统的设计充分体现了"零框架依赖"的理念，所有代码都可调试和修改，为生产环境提供了可靠的技术基础。通过合理的配置和监控，系统能够稳定运行在24/7环境中，为用户提供持续的智能服务。